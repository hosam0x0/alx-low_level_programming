#include "main.h"
/**
 * _abs - Computse the absolute value of N INTWGER
 * @t: the integer to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
